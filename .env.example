# ─── Supabase ─────────────────────────────────────────────────────────────────
# Get these from: Supabase Dashboard → Settings → API
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# Optional dedicated HMAC secret for signed module-review timers. When omitted,
# the server-side Supabase service-role key is used as the signing secret.
TRAINING_COMPLETION_SECRET=generate-a-long-random-secret

# ─── SpecOS AI ────────────────────────────────────────────────────────────────
ANTHROPIC_API_KEY=your-anthropic-api-key

# ─── Database ─────────────────────────────────────────────────────────────────
# Get this from: Supabase Dashboard → Settings → Database → Connection string (URI)
# Use the "Transaction" pooler connection string for serverless (Vercel)
# Format: postgresql://postgres.[project-ref]:[password]@aws-0-[region].pooler.supabase.com:6543/postgres?pgbouncer=true
DATABASE_URL=postgresql://postgres:password@localhost:5432/ditch_training
# Direct connection for migrations (non-pooled)
DIRECT_URL=postgresql://postgres:password@localhost:5432/ditch_training
